[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
bevy = {version = "0.19.0", features = ["debug"]}

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3